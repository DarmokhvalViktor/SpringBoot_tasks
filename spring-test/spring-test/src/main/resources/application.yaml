spring:
  config:
    import: classpath:secrets.yaml
  data:
    mongodb:
      database: "carDB"
      uri: mongodb+srv://admin2:admin2@cluster0.ovbqhpx.mongodb.net/?retryWrites=true&w=majority
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.jdbc.DataSourceAutoConfiguration
      - org.springframework.boot.autoconfigure.orm.jpa.HibernateJpaAutoConfiguration
  # Disable datasource autoconfiguration
#  datasource:
#    driver-class-name: com.mysql.cj.jdbc.Driver
#    url: jdbc:mysql://localhost:3306/test
#    username: root
#    password: "!P@$$word1995"
#  jpa:
#    properties:
#      hibernate:
#        dialect: org.hibernate.dialect.MySQLDialect
#    hibernate:
#      ddl-auto: update
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mail:
    host: smtp.gmail.com
    port: 587
    username: ${mail.user}
    password: ${mail.password}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  security:
    oauth2:
      client:
        registration:
          github:
            client-id: ${security.oauth2.client.registration.github.client-id}
            client-secret: ${security.oauth2.client.registration.github.client-secret}